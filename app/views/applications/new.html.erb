
<%= form_tag "/applications/#{@application.id}" do %>
 <%= select_tag 'user_id', options_from_collection_for_select(@users, :id, :name), class: 'form-select'%>
 <%= submit_tag 'Submit Application' %>
<% end %>
