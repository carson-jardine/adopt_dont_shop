<h1>All Pets</h1>

<% @pets.each do |pet| %>
  <h2><a href="/pets/<%= pet.id %>"><%= pet.name %></a></h2>
    <img src="<%= pet.image %>" />
    <ul>
      <li><%= pet.approximate_age %></li>
      <li><%= pet.sex %></li>
      <li><%= pet.shelter_id %></li>
    </ul>
<% end %>
